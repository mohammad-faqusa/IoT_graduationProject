# Initialise peripherals dictionary
peripherals = {}

# Instantiate each peripheral
peripherals["dht sensor"] = DHTSensor(12)                # GPIO12
peripherals["encoder"] = Encoder(32, 33, 0, 180)         # GPIO32, GPIO33
peripherals["internal led"] = InternalLED()              # GPIO2
peripherals["relay"] = Relay(26)                         # GPIO26