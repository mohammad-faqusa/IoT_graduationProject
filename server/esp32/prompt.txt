# Initialise peripherals dictionary
peripherals = {}

# Instantiate each peripheral
peripherals["internal led"] = InternalLED(simulate=False)  # GPIO2
peripherals["servo motor"] = Servo(13, 50, 500, 2500, 180)  # GPIO13
peripherals["relay"] = Relay(27, True, False)  # GPIO27
peripherals["push button"] = PushButton(5, 20)  # GPIO5